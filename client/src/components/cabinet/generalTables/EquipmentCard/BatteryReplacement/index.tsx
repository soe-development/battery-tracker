import TWrapper from "@/components/cabinet/TableComponents/TWrapper";
import { Box } from "@mui/material";

const BatteryReplacement = () => {
  return <TWrapper bottomTable={"battery-replacement"} />;
};

export default BatteryReplacement;
